# python generated files
__pycache__/
*.py[oc]
build/
# dist/ holds ONLY the published worker OTA bundle (signed release the fleet pulls);
# everything else that lands there (uv build output) stays ignored.
dist/*
!dist/workers.tar.gz
!dist/workers.tar.gz.sig
!dist/workers.version
!dist/workers.rollout.json
wheels/
*.egg-info

# venv
.venv

# env
*.env

# certs folder
certs/

# logs folder
logs/

# editor
.vscode/
.idea/
pyrightconfig.json

snapshot_report.html
*.afdesign
*.afdesign*
*.afphoto
*.afphoto*
.ruff_cache
